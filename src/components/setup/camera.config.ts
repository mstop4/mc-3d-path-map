export const viewScale = 4;
export const ratios = {
  zoomToScaleGUI: 0.5133420832795057, // used to calculate the relationship between camera zoom and the scale on the gui
  isometricZoom: 825, // used to calculate the default zoom level for isometric view
  windowHeight: 921,
};
export const guiScaleSize = 100;
export const autoRotateSpeed = -2;
